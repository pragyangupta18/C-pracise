// program to determine if the number is dividible by 97 that is to get the remainder .( no looping statement used )
#include <stdio.h>
int main(){
    int num ;
    printf("enter the number ");
    scanf("%d",&num);
    printf("divisibility test returns%d\n",num%97);
    return 0;
}
